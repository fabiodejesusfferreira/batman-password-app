import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 23,
    color: "#E5BF3C"
  },
  logo: {
    resizeMode: "contain",
    height: 150,
  }
});